# Local dev: leave unset — Vite's dev proxy forwards /api to the local server.
# Production (set in the Vercel dashboard, Project Settings -> Environment Variables):
# VITE_API_URL=https://your-railway-app.up.railway.app/api
