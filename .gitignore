node_modules/
.expo/
dist/
web-build/
web-dist/
.env
.env.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Native project folders — managed by expo prebuild, not committed to git
android/
ios/

# License admin secrets — never commit the private signing key, the customer
# ledger, or the local admin-panel config (holds the real admin password)
scripts/license-admin/keys/
scripts/license-admin/licenses.json
license-admin-panel/server/config.local.json

# Online Store Subscription admin secrets — completely separate keypair/ledger from
# the ManagerX license system above, same reasoning
scripts/online-store-subscription-admin/keys/
scripts/online-store-subscription-admin/subscriptions.json

# Online Store backend — never commit the store ledger (has API key hashes), the
# local server config, or uploaded product/logo images
online-store/server/config.local.json
online-store/server/data/

# Next.js (online-store/admin) — build output and generated files
.next/
*.tsbuildinfo
next-env.d.ts