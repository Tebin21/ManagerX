node_modules/
.expo/
dist/
web-build/
.env
.env.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Native project folders — managed by expo prebuild, not committed to git
android/
ios/

# License admin secrets — never commit the private signing key, the customer
# ledger, or the local admin-panel config (holds the real admin password)
scripts/license-admin/keys/
scripts/license-admin/licenses.json
license-admin-panel/server/config.local.json

# Online Store backend — never commit the store ledger (has API key hashes) or
# the local server config
online-store/server/config.local.json
online-store/server/data/stores.json