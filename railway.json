{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix license-admin-panel/server && npm run build --prefix license-admin-panel/server"
  },
  "deploy": {
    "startCommand": "npm run start --prefix license-admin-panel/server",
    "restartPolicyType": "ON_FAILURE"
  }
}
