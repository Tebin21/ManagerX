# Production (set this in the Vercel project's Environment Variables, not here):
VITE_API_BASE_URL=https://api.froshiar.store

# Local dev: leave unset — requests fall back to a relative path that Vite's dev
# server proxies to http://localhost:4100 (see vite.config.ts).
